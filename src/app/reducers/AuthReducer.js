
const initialState = {
    byId: {},
    allIds: [],
    isLoading: false,
};

export default function CategoryCourseAvailableReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};
